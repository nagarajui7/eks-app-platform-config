apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: app_tenant_deploy_cap-prod
  namespace: kube-public
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: cap-prod
    spec:
      containers:
      - name: app_tenant_pod_cap-prod
        image: cap-prod:latest
        ports:
        - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: app_tenant_svc_cap-prod
  labels:
    name: app_tenant_cap-prod
  namespace: kube-public
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30001
  selector:
    name: cap-prod
